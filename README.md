# CDSL smoke-test skills

Three APM packages (converted from FreedomIntelligence/OpenClaw-Medical-Skills)
used to smoke-test CDSL repository registration + intake. Each dir is one APM
package: `apm.yml` + `skills/<name>/SKILL.md`.
