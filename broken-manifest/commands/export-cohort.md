---
name: export-cohort
description: Export a filtered patient cohort to a CSV file.
---

# Export cohort

Export the current cohort selection to a CSV file for downstream analysis.

Usage: `/export-cohort --filter "age > 65" --out cohort.csv`
